@using Ext.Net;
@using Ext.Net.MVC;
@using iDAS.Web.ExtExtend;
@model iDAS.Items.DepartmentRegulatoryItem
@(Html.X().Window().Height(1).Maximized(true)
    .Title("Nội dung")
    .Icon(Icon.Zoom)
    .Layout(LayoutType.Fit).BodyPadding(2)
    .Items(
                Html.X().TextAreaFor(m => m.Content).MinHeight(200).Flex(1).HideLabel(true).ReadOnly(true)
        )
.Buttons(Html.X().ButtonExit())
)