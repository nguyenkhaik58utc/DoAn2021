@using Ext.Net.MVC
@using Ext.Net
@(Html.X().Window()
    .ID("WindowAuditUpdate")
    .Title("Xem chi tiết kết quả đánh giá")
    .Icon(Icon.Zoom)
    .Height(1)
    .Maximized(true)
    .Constrain(true)
    .Layout(LayoutType.Border)
    .Modal(true)
    .BodyPadding(0)
    .Border(true)
    .Resizable(false)
    .Center()
    .Items(
            Html.X().Panel()
                .Region(Region.West)
                .Width(400)
                .Split(true)
                .Border(true)
                .Margin(1)
                .TitleAlign(TitleAlign.Center)
                .Layout(LayoutType.Fit)
                .Collapsible(true)
                .Header(false)
                .ItemsFromAction("Detail", "Audit", new { area = "Audit" }),
            Html.X().Panel()
                .Header(false)
                .TitleAlign(TitleAlign.Center)
                .Collapsible(true)
                .Region(Region.Center)
                .Border(true)
                .Margin(1)
                .Split(true)
                .Layout(LayoutType.Fit)
                    .ItemsFromAction("ResultDetail", "Audit", new { Area = "Audit" })
    )
    
)