@using Ext.Net
@using Ext.Net.MVC
@using iDAS.Utilities
@using iDAS.Web.ExtExtend
@model iDAS.Web.Areas.Human.Models.V3_Category.V3_EducationResultResponse
@(Html.X().Window()
    .ID("DetailEducationResultWindow")
    .Title("Thông tin kết quả đào tạo")
    .Icon(Icon.Pencil)
    .BodyPadding(0)
    .Width(550)
    .Constrain(true)
    .Modal(true)
    .Layout(LayoutType.Fit)
    .Maximizable(true)
    .Border(false)
    .Items(
        Html.X().FormPanel()
            .ID("DetailEducationResultForm")
            .Url(Url.Action("Update"))
            .Layout(LayoutType.Form)
            .Frame(true)
            .Border(false)
            .MarginSpec("0 10 10 10")
            .StyleSpec("border: none;")
            .FieldDefaults(df => df.LabelWidth = 100)
            .DefaultAnchor("100%")
            .Items(
                                Html.X().TextAreaFor(m => m.ID).Hidden(true),
                                Html.X().TextFieldFor(m => m.Name).FieldLabel("Tên kết quả đào tạo" + Html.Required())
                                    .EmptyText("Nhập tên kết quả đào tạo").ReadOnly(true).LabelWidth(130)
                                    .BlankText("Bạn phải nhập tên kết quả đào tạo!")
                                    .AllowBlank(true).AllowOnlyWhitespace(false)
            )
    )
)

