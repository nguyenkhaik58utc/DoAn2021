@using Ext.Net
@using Ext.Net.MVC
@using iDAS.Utilities
@using iDAS.Web.ExtExtend
@model iDAS.Web.Areas.Human.Models.V3_Category.V3_FamilyRelationShipResponse
@(Html.X().Window()
    .ID("UpdateFamilyRelationShipWindow")
    .Title("Sửa thông tin loại quan hệ gia đình")
    .Icon(Icon.Pencil)
    .BodyPadding(0)
    .Width(550)
    .Constrain(true)
    .Modal(true)
    .Layout(LayoutType.Fit)
    .Maximizable(true)
    .Border(false)
    .Items(
        Html.X().FormPanel()
            .ID("UpdateFamilyRelationShipForm")
            .Url(Url.Action("Update"))
         .Layout(LayoutType.Form)
    .Frame(true)
    .Border(false)
    .MarginSpec("0 -4 -3 -4")
    .StyleSpec("border: none;")
    .FieldDefaults(df => df.LabelWidth = 110)
    .DefaultAnchor("100%")
            .Items(
                                Html.X().TextAreaFor(m => m.ID).Hidden(true),
                                Html.X().TextFieldFor(m => m.Name).FieldLabel("Tên loại quan hệ" + Html.Required())
                                    .EmptyText("Nhập tên loại quan hệ gia đình")
                                    .BlankText("Bạn phải nhập tên loại quan hệ gia đình!")
                                    .AllowBlank(true).AllowOnlyWhitespace(false).Width(520).MarginSpec("0 8 0 8")
            )
    )
    .Buttons(
        Html.X().ButtonSave().Handler("updateFamilyRelationShipSubmit"),
        Html.X().ButtonExit().Handler("this.up('window').close();")
    )
)
<script>
    var updateFamilyRelationShipSubmit = function () {
        App.UpdateFamilyRelationShipForm.submit({
            waitMsg: "Đang thực hiện ....",
            success: function (result) {
                if (result) {
                    reloadFamilyRelationShip();
                    App.UpdateFamilyRelationShipWindow.close();
                }
            }
        });
    };
</script>
