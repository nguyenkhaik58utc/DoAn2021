@using Ext.Net
@using Ext.Net.MVC
@using iDAS.Utilities
@using iDAS.Web.ExtExtend
@model iDAS.Web.Areas.Human.Models.V3_Category.V3_DisciplineCategoryResponse
@(Html.X().Window()
    .ID("DetailDisciplineCategoryWindow")
    .Title("Thông tin hình thức kỉ luật")
    .Icon(Icon.Pencil)
    .BodyPadding(0)
    .Width(550)
    .Constrain(true)
    .Modal(true)
    .Layout(LayoutType.Fit)
    .Maximizable(true)
    .Border(false)
    .Items(
        Html.X().FormPanel()
            .ID("DetailDisciplineCategoryForm")
            .Url(Url.Action("Update"))
                    .Layout(LayoutType.Form)
    .Frame(true)
    .Border(false)
    .MarginSpec("0 -4 -3 -4")
    .StyleSpec("border: none;")
    .FieldDefaults(df => df.LabelWidth = 130)
    .DefaultAnchor("100%")
            .Items(
                                Html.X().TextAreaFor(m => m.ID).Hidden(true),
                                Html.X().TextFieldFor(m => m.Name).FieldLabel("Tên hình thức kỉ luật" + Html.Required())
                                    .EmptyText("Nhập tên hình thức kỉ luật")
                                    .BlankText("Bạn phải nhập tên hình thức kỉ luật!")
                                    .AllowBlank(true).AllowOnlyWhitespace(false).ReadOnly(true).Width(520).MarginSpec("0 8 0 8")
            )
    )
)
