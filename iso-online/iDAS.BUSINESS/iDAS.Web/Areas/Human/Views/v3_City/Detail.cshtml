@using Ext.Net
@using Ext.Net.MVC
@using iDAS.Utilities
@using iDAS.Web.ExtExtend
@model iDAS.Web.Areas.Human.Models.V3_Category.V3_CityResponse
@(Html.X().Window()
    .ID("DetailCategoryCityWindow")
    .Title("Thông tin thành phố")
    .Icon(Icon.Pencil)
    .BodyPadding(0)
    .Width(550)
    .Constrain(true)
    .Modal(true)
    .Layout(LayoutType.Fit)
    .Maximizable(true)
    .Border(false)
    .Items(
        Html.X().FormPanel()
            .ID("DetailCategoryCityForm")
            .Url(Url.Action("Update"))
            .Layout(LayoutType.Form)
            .Frame(true)
            .Border(false)
            .MarginSpec("0 10 10 10")
            .StyleSpec("border: none;")
            .FieldDefaults(df => df.LabelWidth = 100)
            .DefaultAnchor("100%")
            .Items(
                                Html.X().TextAreaFor(m => m.ID).Hidden(true),
                                Html.X().TextFieldFor(m => m.Name).FieldLabel("Tên thành phố" + Html.Required())
                                    .EmptyText("Nhập tên thành phố").ReadOnly(true)
                                    .BlankText("Bạn phải nhập tên thành phố!")
                                    .AllowBlank(true).AllowOnlyWhitespace(false)
            )
    )
)
