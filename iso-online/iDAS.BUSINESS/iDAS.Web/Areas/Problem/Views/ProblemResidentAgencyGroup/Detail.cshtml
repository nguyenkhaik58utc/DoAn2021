@using Ext.Net
@using Ext.Net.MVC
@using iDAS.Utilities
@using iDAS.Web.ExtExtend
@model iDAS.Web.Areas.Problem.Models.ProblemResidentAgencyGroupDTO
@(Html.X().Window()
    .ID("DetailProblemResidentAgencyGroupWindow")
    .Title("thông tin nhóm cơ quan thường trú")
    .Icon(Icon.Pencil)
    .BodyPadding(0)
    .Width(550)
    .Constrain(true)
    .Modal(true)
    .Layout(LayoutType.Fit)
    .Maximizable(true)
    .Border(false)
    .Items(
        Html.X().FormPanel()
            .ID("DetailProblemResidentAgencyGroup")
            .Layout(LayoutType.Fit)
            .StyleSpec("border: none;")
            .Border(false)
            .Items(
                Html.X().TabPanel().ActiveTabIndex(0).Border(true)
                    .Items(
                        Html.X().FormPanel()
                            .Layout(LayoutType.VBox)
                            .LayoutConfig(new VBoxLayoutConfig { Align = VBoxAlign.Stretch })
                            .Title("Thông tin chi tiết")
                            .BodyPadding(5)
                            .Frame(true)
                            .StyleSpec("border: none;")
                            .FieldDefaults(d => d.LabelAlign = LabelAlign.Left)
                            .Items(
                                Html.X().TextFieldFor(m => m.Name).FieldLabel("Tên trạng thái sự cố"),
                                Html.X().TextAreaFor(m => m.Description).FieldLabel("Mô tả")
                            )
                    )
            )
    )
)
s