@using Ext.Net.MVC
@using Ext.Net
@using iDAS.Utilities
@using iDAS.Web.ExtExtend
@model iDAS.Items.QualityCriteriaCategoryItem
@{
    var hiddenAuditor = ViewData["hiddenAuditor"];
}
@(Html.X().Window()
                .ID("winDetailCriteriaCategory")
                .Title("Xem chi tiết thông tin bộ tiêu chí đánh giá")
                .Icon(Icon.Zoom)
                   .Height(270)
          .Width(550)
                .BodyPadding(6)
                .Constrain(true)
                .Modal(true)
                .Layout(LayoutType.Fit)
                .Items(
                Html.X().FormPanel()
                .ID("frCriteriaCategory")
                 .Layout(LayoutType.VBox)
                                .LayoutConfig(new VBoxLayoutConfig { Align = VBoxAlign.Stretch })
                .Frame(true)
                .StyleSpec("border: none;")
                .FieldDefaults(d => d.LabelAlign = LabelAlign.Left)
                .Items(
                        Html.X().HiddenFor(m => m.ID),
                                Html.X().TextFieldFor(m => m.Name).FieldLabel("Tên bộ tiêu chí").ReadOnly(true).LabelAlign(LabelAlign.Top)
                        .BlankText("Tên bộ tiêu chí không được trống!").AllowOnlyWhitespace(false),
                                               Html.X().EmployeeFieldFor(m => m.Audit).ReadOnly(true).Hidden((bool)hiddenAuditor).Title("Người đánh giá"),
                    Html.X().CheckboxFor(m => m.IsUse).BoxLabel("Sử dụng").ReadOnly(true).HideLabel(true)            
                        //Html.X().CheckboxFor(m => m.IsActive)
                        //        .BoxLabel("Kích hoạt")
                        //        .HideLabel(true)
                        //        .MarginSpec("0 0 0 110"),
                        //Html.X().TextFieldFor(m => m.CreateByName)
                        //        .FieldLabel("Người tạo")
                        //        .ReadOnly(true),
                        //Html.X().TextFieldFor(m => m.CreateAt)
                        //        .FieldLabel("Ngày tạo")
                        //        .ReadOnly(true),
                        //Html.X().TextFieldFor(m => m.UpdateByName)
                        //        .FieldLabel("Người cập nhật")
                        //        .ReadOnly(true),
                        //Html.X().TextFieldFor(m => m.UpdateAt)
                        //        .FieldLabel("Ngày cập nhật")
                        //        .ReadOnly(true)
               )
            )
            .Buttons(           
            Html.X().Button()
            .ID("btnCancel")
            .Text("Thoát")
            .Icon(Icon.Decline)
            .TabIndex(20)
            .Handler("App.frCriteriaCategory.reset(); this.up('window').close();")
            )
)